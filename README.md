
  # User Dashboard Design (Copy)

  This is a code bundle for User Dashboard Design (Copy). The original project is available at https://www.figma.com/design/CGf2W2NYglR6imdxo0JDaE/User-Dashboard-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  